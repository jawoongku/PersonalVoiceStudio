model_dir: /Users/jawoongku/Models/Fun-CosyVoice3-0.5B
dataset_dir: data/my_voice_prepared
run_dir: artifacts/runs/my_voice
device: mps
upstream_root: /Users/jawoongku/CosyVoice

training:
  batch_size: 1
  grad_accum_steps: 8
  learning_rate: 0.0001
  weight_decay: 0.01
  max_epochs: 20
  grad_clip: 1.0

lora:
  target_modules: [q_proj, k_proj, v_proj, o_proj]
  rank: 16
  alpha: 64
  dropout: 0.05
