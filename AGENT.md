# Agent 작업 지침

## 프로젝트 목표

macOS에서 사용자가 직접 목소리를 녹음하고, 품질을 확인한 뒤 개인 Voice Package를 학습·관리·TTS에 사용하는 로컬 우선 애플리케이션을 만든다.

## 기술 기준

- UI: 최종적으로 SwiftUI macOS 앱
- 음성 엔진: Python, PyTorch, CosyVoice3
- 초기 UI 검증: Gradio
- 연산 경계: SwiftUI는 사용자 경험과 녹음, Python은 데이터·학습·추론
- Apple Silicon/MPS를 우선하되 CPU fallback은 명시적으로 표시한다.
- CUDA, 원격 GPU, 외부 음성 API를 기본 경로로 추가하지 않는다.

## 작업 규칙

1. 작업 시작 전 `INDEX.md`와 관련 명세 문서를 읽는다.
2. 사용자 요청과 첨부 지시문을 구분한다.
3. 실제로 검증하지 않은 기능을 완료로 표시하지 않는다.
4. 사용자 음성 원본은 수정하거나 삭제하지 않는다. 제외 시 복구 가능한 위치로 이동한다.
5. 음성 품질은 파일 유효성, 발음 일치, 화자 유사도를 별도로 기록한다.
6. MPS, CPU, synthetic fixture, 실제 사용자 데이터 결과를 혼동하지 않는다.
7. 변경 후 관련 테스트와 문서 상태를 갱신한다.
8. UI 작업은 녹음 권한, 학습 중 취소, 오류 표시, 파일 저장을 함께 설계한다.

## 완료 보고 형식

- 변경한 파일
- 실행한 검증
- 성공한 범위
- 아직 차단된 범위
- 사용자가 다음에 해야 할 일

## 금지 사항

- 사용자 승인 없이 원본 음성 삭제
- 학습되지 않은 adapter를 최종 품질 모델로 표현
- CPU smoke 결과를 MPS 성공으로 표현
- transcript가 없는 음성을 학습 데이터로 자동 확정
- UI에서 긴 학습을 메인 스레드에 직접 실행
